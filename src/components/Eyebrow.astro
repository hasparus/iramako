---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<p class:list={["mb-6 font-hand text-[clamp(1.25rem,2vw,1.6rem)] leading-[1.15] text-primary", className]}>
  <slot />
</p>
