---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["mx-auto w-full max-w-[1500px] px-5 sm:px-[clamp(24px,4vw,58px)]", className]}>
  <slot />
</div>
