---
import Container from "./Container.astro";
import Logo from "./Logo.astro";
import NavLink from "./NavLink.astro";

interface Props {
  instagramUrl: string;
}

const { instagramUrl } = Astro.props;
---

<header class="relative z-40 border-b border-rule">
  <Container class="flex flex-col gap-5 pb-5 pt-5 sm:flex-row sm:items-center sm:justify-between sm:gap-8 sm:py-7">
    <Logo class="w-[42px] sm:w-[clamp(41px,3.33vw,54px)]" />
    <nav
      class="flex flex-wrap items-center gap-x-5 gap-y-2 text-[0.92rem] leading-tight text-ink sm:justify-end sm:gap-x-[clamp(18px,4vw,72px)] sm:text-[clamp(.9rem,1.1vw,1.08rem)]"
      aria-label="główna"
    >
      <NavLink href="#skladniki" splash="arbuz">składniki</NavLink>
      <NavLink href="#kosmetyki" splash="kobalt">kosmetyki</NavLink>
      <NavLink href={instagramUrl} target="_blank" rel="noreferrer" splash="kolko13">instagram</NavLink>
      <NavLink href="#kontakt" splash="szary">kontakt</NavLink>
    </nav>
  </Container>
</header>
